spring:
  application:
    name: gateway-graphql
  graphql:
    graphiql:
      enabled: true
    path: /graphql

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info

